import React from 'react'
// Install is now handled by the header button (MobileHeader.tsx).
// This component is kept as a no-op to avoid import errors.
export const InstallPrompt: React.FC = () => null
